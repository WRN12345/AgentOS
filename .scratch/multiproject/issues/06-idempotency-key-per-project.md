# 06 — 幂等键并入项目

**What to build:** 幂等唯一键纳入项目维度——同一用户、同一键、同一请求路径，在 A/B 不同项目下视为不同请求，不复用响应。

**Blocked by:** 01 — 项目上下文与 admin 全局化

**Status:** ready-for-agent

- [ ] 项目 A 的幂等请求在项目 B 用同键 → 不返回 A 的缓存响应，独立执行
- [ ] 同项目内同键仍复用（既有行为不变）
